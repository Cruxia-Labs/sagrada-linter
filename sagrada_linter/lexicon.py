"""Display-string lexicon (vendored consumer of the design system's
docs/design/lexicon.json — provenance: cruxia-engine @ foundry 2026-07-26).

Brand words are DATA until the season's first public drop: every user-facing
display string in this package reads from here, never a literal, so a rename
is one edit. The typed surface (command verbs, package name) is permanent by
founder ruling and does NOT live here.
"""

LEX = {
    "experience": "Examen",             # the full reading — the ACT (ruled 2026-07-26)
    "witness": "Communion",             # the transcript layer — the WITNESS
    "artifact": "Epitaph",              # the dropped single-finding card
    "artifact_plural": "Epitaphs",
    "dark_page": "the Graveyard",
    "ceremony": "Quietus",              # never typed, never a command
}

# The folk vocabulary (zombie / belief rot / walking / laid to rest) is
# LEXICON.md canon — frozen with the band ladder, not swappable, used freely.
